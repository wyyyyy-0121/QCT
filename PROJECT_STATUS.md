# FormulaGuard 项目状态

更新时间：2026-08-31

## 当前权威状态（2026-08-31）

- `V4-R1` 是唯一冻结主排序器；`V4.2-Review-B` 只保留为 V4 系辅助复核位。
- `V4.1-PCG`、`V4.3-Semantic`、`V5-Core`、`V5-Core R2` 和 `V5-PSL` 均已结束，
  其正负结果和原始哈希保留为历史证据，不再调参或改名恢复。
- 当前研究线 `model-discovery` 已完成 Phase 0、Gate 1、Gate 2 评分和 RQ-C；Gate 2A
  与 Gate 2B 均未通过，Branch C 也未形成可部署的无标签继任模型。`V4-R1` 仍是唯一
  冻结主排序器，不得在同一批揭晓标签上继续调参或创建 `V5-R1`。
- 后续预注册的 `CWRP` 跨工作簿角色先验已在 303 个去重真实工作簿、30,875 个遮蔽
  目标上完成五折自监督检验。U0 通过，但 U1/U2 失败：工作簿宏 Top-5 为 7.81%，
  低于 `context_frequency` 的 21.58%；选择性 Top-5 为 69.83%，覆盖仅 5.23%，
  等频 10 桶 ECE 为 20.23%。该研究线按预注册停止，不实现定位候选、不创建 `V5-R1`。
- CWRP 首份 v1 回执错误使用等宽 ECE；原回执永久保留。提交 `674b6df` 只把统计
  修正为预注册的等频 ECE，校正 v2 的逐目标预测哈希与 v1 完全相同，因此不是结果后
  调参。完整记录见 `research/V5_CWRP_RESULT.md`。
- CWRP 失败后预注册的 `DRFV` 已完成 input-only 语料门。SpreadsheetBench v1 的
  2,726 个 input 中只有 625 个达到单簿解析门；删除重复和已知重合后有 607 个工作簿、
  219 个结构组和 74,570 个可解析公式。工作簿覆盖 22.93%（门为 80%）、公式数低于
  100,000，两项 U0 失败。DRFV 按预注册在训练前停止，未读取 answer、未接收
  SpreadsheetBench 2、未产生模型权重。完整记录见 `research/V5_DRFV_U0_RESULT.md`。
- 新保管人保密 `240+120` 已由用户放在仓库外
  `/home/ayaka/code/FormulaGuard_240_120/`；当前未列目录、未哈希、未读取。用户已授权：
  当新候选规格、源码与配置均已提交冻结，且完整预测锁已生成并提交后，Codex 可直接
  执行一次受保护集验证，无需再次等待用户确认或要求用户手工投喂。CWRP 已失败，未达到
  这些解锁条件，因此该目录继续封存。
- Phase 0 产物：
  `research/V5_MODEL_DISCOVERY_CURRENT_STATUS.md`、
  `research/V5_MODEL_DISCOVERY_TASK_CONTRACT.md`、
  `research/V5_MODEL_DISCOVERY_DATA_LEDGER.json`、
  `research/V5_MODEL_DISCOVERY_POWER_REPORT.md`。

- Gate 2 与 Branch C 结果：
  `research/V5_MODEL_DISCOVERY_GATE2_RESULT.md`、
  `research/V5_MODEL_DISCOVERY_BRANCH_C_RESULT.md`；机器回执保存在
  `results/model_discovery_gate2_final_v3/` 和 `results/model_discovery_branch_c/`。
- CWRP 负结果：`research/V5_CWRP_RESULT.md` 和
  `research/V5_CWRP_RESULT.json`；本地不可变回执保存在
  `results/cwrp_self_supervised_v1/` 与 `results/cwrp_self_supervised_v2/`。
- 当前功效审计显示，25 个 Enron 结构组或 30 个最终模板组不足以可靠检出 5 个百分
  点的确认性 Top-5 改善；正式盲测前需要增加独立模板，或降低论文结论为探索性估计。

当前执行计划见 `research/FORMULAGUARD_MODEL_DISCOVERY_PLAN_V3.md`，基础协议见
`research/V5_CORE_FIRST_PRINCIPLES_DISCOVERY_PLAN.md`。下方内容按日期
保留，只用于复现历史过程，不覆盖上述当前状态。

## 历史记录：2026-08-30 及更早

- `V4-R1`仍是唯一冻结主排序器；`V4.2-Review-B`是冻结辅助复核模块。
- 初代候选中心`V5-Core`已完成实现，但因锁定验证的相对公式变换16/16重合且
  Enron安全回归失败而否决，不得再写成“尚未实现”。
- `V5-Core R2`随后经历DNCA、R2-R1、R2-R2、祖先修复和制度证据支配保护。
  最终固定配置在历史100例和Enron压力测试中未通过Top-5、MRR和DCF伤害门槛，
  冻结路径已经终止；这些名称都是开发研究阶段，不是正式版本。
- 下一条研究线现使用开发标识`V5-PSL-dev1-rev1`，研究静态排名锚定、修复特异性
  验证和显式可识别性门控。它不继承R2权重，只有全新第三方锁定验证通过后才可成为正式V5。
- `V5-PSL-dev1-rev1`的核心实现、12个成对扰动、内部/LibreOffice双求值、四种选择性
  状态、四项固定消融、完整排名接口和Linux CLI已经完成；这些只证明代码可运行。
- 候选下游证据已收紧为“朝角色参照移动”的定向恢复；等距、远离参照、缺少参照
  或求值失败均记零，并以局部改善与定向恢复的几何平均形成联合指标；安慰剂使用
  同一指标，不能再把任意下游数值变化误算为恢复。
- 六个公开语料的固定来源/许可清单、下载与任务边界适配器、公开压力审计、候选锁、
  第三方240错误+120控制打包、四方法无标签预测、独立预测锁和一次性聚类评分工具
  已实现。所有正式入口都保留`unsupported`案例并拒绝提前使用SECRET。
- 第三方协议已在任何公开压力、候选锁和第三方结果之前升级为v2：只要求一名未参与
  模型开发的独立保管评测人，不再要求六名制题人和两名复核者；仍保留30个未见模板、
  240错误+120控制、案例计划/模板去重/许可/脱敏证据哈希和PUBLIC/SECRET预承诺。
  该证据只能称为“单保管人盲持的外部模板确认”，不得声称六人或多中心验证。
  预测锁会独立重跑全部模型并比较除计时外的完整语义输出，换序或证据篡改均拒绝。
- 常规短测试共收集246项，其中245项通过、1项合成全链路测试默认跳过，当前墙钟约
  25秒；v2合成全链路已单独启用并在约14秒内完成360例打包、预测、独立重算锁定和
  1,440个方法事件评分。临时输出已删除，此结果只证明工程入口贯通，不是公开压力、
  候选锁或第三方研究证据。
- 文献门已在任何公开压力结果和候选锁之前升级为v3：七个方向的11篇可获得主文献
  已完成全文核对；Wang 2021和Hofer/Wotawa 2013保留为2篇封闭访问披露，不能支持
  原创或优越性。回执明确写作“11篇全文+2篇披露”，不是13/13全文，并已`passed=true`。
- 首轮公开压力已在提交`71459f5`完成90例和450个方法事件，但预审计显示默认方法
  Top-5为35%、控制行动率为30%、每百检查格效率为5.43，未通过60%、15%和不低于
  去扰动版本8.54的三项门槛。正式审计又因约`1e-15`的浮点复现噪声在生成回执前
  拒绝；进一步复算还发现两个案例的第17/18位换序。完整记录见
  `V5_PSL_PUBLIC_PRESSURE_FAILURE_1.md`，当前尚未消耗唯一一次机制修订。
- 首轮失败后已先固定开发修订：90例降级为已揭晓调参集，只比较原默认参数、11组向
  保守方向移动的参数和无扰动基线，并按原始工作簿哈希做五折稳定性检查；同集后续结果不得再称为
  预登记压力通过，真实240+120仍是唯一正式晋级证据。
- 有界调参已在提交`c60b7ae`完成1170个分片且哈希复核一致；12组参数全部失败，最佳
  Top-5仅38.33%，所有配置控制行动率仍为30%，不得选择近失配置。根因显示一般响应
  强度优先排序会破坏静态Top-5，且8个安慰剂使0.10强证据尾概率不可达。唯一结构修订
  随后完成90例和独立重算审计：静态Top-5恢复至63.33%，但localized覆盖为0%，控制
  行动率36.67%，效率1.90低于静态复核8.54，且五折仅2/5稳定。修订未通过固定门槛，
  `V5-PSL`研究线已终止；证据见`V5_PSL_PUBLIC_PRESSURE_FAILURE_2.md`。
- 已找到一名新的独立保管评测人并由其制作全新数据；目前内容、标签和承诺哈希均未
  释放。由于`V5-PSL`已终止，不再生成`v5-psl-candidate-lock`，新数据只可在未来
  继任候选及协议冻结后使用；旧v1包不能作为该确认集。
- `V5_SUCCESSOR_BASELINE_DIAGNOSTIC.md`固定的90例继任基线已用24进程完成；V4、
  V4.3和静态锚定Top-5分别为56.67%、58.33%和63.33%。四种固定选择性规则均未
  完整过门；最接近的“V4 Top-5恰好一个strong”命中17/60、错误行动精度89.47%、
  干净控制0/30，但28.33%源命中低于30%固定线，不能选择为通过。
- 用户已把旧`240+120_六制题人最终交付`降级为不保密试用数据；它只能按
  `V5_SUCCESSOR_REVEALED_TRIAL_PROTOCOL.md`做一次固定复现，不能称外部验证。
  另一名新保管人制作的全新数据保持未见，且必须与90例和旧试用包模板不重合。
- 旧v1包只读接收审计器已实现：在不解压、不调用模型的条件下核对目录哈希、ZIP
  路径安全、PUBLIC/SECRET承诺、240+120设计、30模板、双复核和720份工作簿映射；
  人员身份独立性不会仅凭包内ID被误记为已验证。

下方按日期保留的“下一步”均是当时的历史记录，不再代表当前命令。当前结论是：
不得继续调参或修订`V5-PSL`。下一条主模型研究线必须使用新的名称、机制主张和
预登记；90例和旧240+120试用包都只能作为已揭晓开发数据，新保管人数据必须保持未见。
只有开发候选确定后，才进入
保管人预提交双归档哈希 -> 候选锁 -> 释放PUBLIC -> 完整预测锁 -> 释放SECRET并只评分一次。

## 研究目标

在没有正确输出真值、错误公式仍返回正常数值时，利用公式依赖图、候选修复和反事实干预，定位引发下游传播的静默源公式错误。项目按高中生竞赛的本科课程设计水平控制范围，内部目标是形成具备银奖竞争力的完整证据链。

## 已完成

- A1公式解析、归一化、依赖图、下游传播路径和可重算工作簿模型；
- 六类候选修复、候选质量元数据和Top-15编辑族配额；
- 公式、图、行为、内部一致性约束和图干预责任分数GIR；
- Random、Excel-like、Pattern、Graph、Behavior、ExceLint-like、WARDER-like及SFL-Oracle参照；
- 五项消融、敏感性、干净表报警、性能、失败案例和报告流水线；
- 标签物理隔离、数据验证、Git锁、冻结配置、文件哈希和完成度审计；
- 工作簿级多进程调度，默认使用约75%的逻辑处理器；
- 超长结果工作簿采用有界预览，完整行仍保留在XLSX中；候选公式按文本写入；
- Enron清点、`.xls`转换和外部评测入口代码。

## v1 已完成但仅作历史基线

冻结版本：提交 `6a91204e1137532301f11356e63fa5fbf519be76`，标签 `freeze-v1`。

- full：864/864实例有效；
- FormulaGuard MRR 0.6899，最强无真值基线0.4696；
- 配对MRR差值约+0.2203，bootstrap 95%区间为正；
- 干净表报警率0%；
- 完成度审计和结果工作簿公式扫描通过。

限制：v1六个测试族的核心依赖结构相同，因此这些数字不能作为最终“跨结构泛化”结论，也不再为导出故障重跑。

## v2 已完成的工程与诊断证据

### 多结构基准

v2实现了层级树、横向链、多表级联、矩阵汇聚、纵向滚动和分叉—汇合六类依赖拓扑。结构审计直接从XLSX重建图，不依赖生成器标签。

困难条件探针 `v2_sparse`：

- 36/36实例有效；
- 六类错误各6例，浅/中/深各12例；
- 六个声明拓扑和六个实算结构签名均不同；
- 覆盖单表、多表、跨表和非跨表结构；
- Candidate Coverage@15为100%；
- FormulaGuard MRR 0.9253，最强无真值基线ExceLint-like为0.6560；
- 配对差值+0.2693，bootstrap 95%区间 `[0.1290, 0.4170]`；
- 正确修复Top-1为100%。

以上36例是定向困难探针，不替代预注册quick和冻结full。

### v2 smoke

- 12/12实例有效，六类错误和三档深度完整；
- 两个开发族的声明拓扑和实算结构签名均不同；
- Candidate Coverage@15和正确修复Top-1均为100%；
- 23项单元测试通过；
- 报告、演示、结果工作簿、公式扫描和完成度审计通过。

smoke中Pattern和WARDER-like优于FormulaGuard，原因是两个可见版本的复制规律很明显；该结果用于确认简单条件下基线确实有竞争力，不用于论文主结论。

### 性能修复

行为异常和内部约束已经共享同一次工作簿求值，消除了每个候选的重复重算，并增加回归测试保证以后不会恢复为双重求值。GPU不参与，因为当前算法是公式解析、图遍历和大量小规模分支求值，适合CPU多进程而非神经网络矩阵运算。

## v2 冻结正式结果

冻结提交：`672a62523ff9c0a36e2b9712bf7a9be3746c8f61`，标签 `freeze-v2`。

- full 864/864 实例有效；MRR 0.9452，Top-1 0.9144，Top-5 0.9896；
- 最强无真值基线 WARDER-like MRR 0.7788，配对差值 +0.1664，95% CI `[0.1422, 0.1899]`；
- 干净表报警率 16.7%，Coverage@15 100%，正确修复 99.4%；
- LibreOffice 交叉验证 906/906 公式值一致；
- 局限：`ablate_graph` MRR 0.9682，高于完整模型约 0.023，因此不得宣称固定局部图异常项有效。

## v3 当前进度

- 已预注册结构自适应反事实责任模型，v2 证据和目录保持只读隔离；
- 已实现局部结构可靠度、净反事实改善、副作用惩罚、候选级下游恢复与路径责任；
- 已增加 Source-before-descendants、Source-first-in-causal-cone、Path Coverage、Repair Safety 和证据强度报警；
- 已建立全新开发、验证和六个测试族，并增加独立控制链与无关合法公式区；
- v3 smoke：12/12 有效，六类错误和三档深度完整，一键流水线与严格审计通过；
- v3 smoke 只作为工程验收：v3 MRR/Top-1/修复率均为 100%，但 v2 同样为 100%，不能作为 v3 优势结论；
- 六测试拓扑探针：36/36 有效，六个声明拓扑和六个实算签名均不同；
- 首轮 v3 quick：48/48 有效，MRR 0.9592、Top-1 0.9583、修复率 0.9583、干净表报警率 0%；相对最强无真值基线的 MRR 差值为 +0.3586，95% CI `[0.2535, 0.4664]`；
- 首轮 quick 同时暴露出固定截断缺陷：两个第 51 位源公式未进入前 50 个干预对象，另有四例传播路径因只保存前 10 个终点而漏报；因此首轮冻结已经作废，完整证据保存在 `results/v3_quick_pre_revision`；
- 已按预注册允许的唯一一次 quick 后修订，将典型工作簿的干预上限提高到 100，并输出全部可达检查/结构终点；未改权重、候选规则、Top-15、阈值目标或数据生成器；
- 修订后 29 项单元与回归测试通过；v3 smoke 12/12 有效，MRR、Top-1、Top-5、路径覆盖和修复安全性均为 100%，结果工作簿公式错误扫描为空。

## 当前未完成

- 本地取得并审计六个公开语料，人工处理旧格式坐标、许可和逐项排除，再运行公开压力与四消融；
- 找到一名独立保管评测人，由其按v2准备并盲持30模板的240错误+120控制；公开压力
  通过且收到双归档预承诺后，提交干净工作区并生成候选锁；
- 以冻结V4主模型、历史负结果和新方法的真实结果完成论文、图表、演示与答辩材料。

## 2026-08-27：V5-Core 核心重构实施状态

- 已新增候选优先的主排序器`formulaguard/v5_core.py`，不调用冻结V4的`v4_scores()`作为排序底座；旧V4.1/V4.2/V4.3接口及历史证据保持不变。
- 已实现公式制度区域、合法例外概率、32候选配额、结构/因果/图恢复/独立复现四个证据族、规则头和带符号约束的成对学习头。
- 已建立开发1200、红队360、干净360、锁定验证480和第三方600例的数据生成、跨划分泄漏审计、标签隔离、原子分片、断点续跑和24进程接口。
- 已建立规则/学习头训练与校准、固定消融、Enron回顾性检查、内部预测锁、模型选择、冻结、性能、第三方打包与盲测评分脚本。
- 内部验证锁定前只运行公开清单/工作簿哈希审计；含标签的数据质量审计与评分严格位于预测锁之后，评分器还会复算元数据和全部分片哈希。
- 当前源码24例工程smoke：规则头Top-5为95.83%、MRR为0.8903、Coverage@32为100%、精确修复率为91.67%；学习头Top-5为100%、MRR为0.9354。重新生成的24份真实分层控制中，两头误报率均为8.33%，合法例外误报率为11.11%，仍分别低于10%和15%的预登记门槛；误报只出现在2份near-tie控制。这些数字只证明工程链路和机制可运行，不作为论文主结论。
- 结果工作簿五个工作表已逐页预览，公式错误扫描为0；下一项大型任务是240例机制pilot。pilot及开发结果只用于机制检查和模型选择，不能冒充独立结论。
- 完成度复查发现早期干净控制的部分“结构”只在元数据名称上不同，已在pilot运行前修正：现在12类控制各有可复算的W:AB结构探针，360份全量审计确认每类30份、12类探针签名互不重叠；240份校准和120份锁定控制在每类中按20/10分层。
- 新的24份分层控制工程检查覆盖12类结构各2份，其中18份为合法例外；数据审计进一步验证每个错误表与原表只差真实源一个公式、正确/错误公式与工作簿一致、无显式计算错误、声明下游确实改变且传播深度可复算。这仍只属于工程安全检查，不替代240/120控制集的正式结果。
- 冻结脚本已升级为完整证据锁：除选中配置外，还会锁定V5-Core源码、历史V4.x源码、四层数据清单、训练/Enron/验证审计、运行环境、Git提交和预登记参数；任何文件缺失、路径错位、工作区未提交或选择回执允许再调参时均拒绝冻结。
- 候选干预预算已按预注册语义修正：每格基础预算严格取全池质量最高2个候选，深度预算先保留最高2个、再覆盖不同候选类别并补齐至8个；候选类别配额只决定32候选池成员，不再意外决定干预优先级。
- 第三方最终集不能由项目生成器自产；仓库外打包器会复算600对原表/错误表的单公式注入、静默数值、真实下游变化和传播深度，并验证独立出题声明、各类人工案例数量、非相邻平移比例、冻结前PUBLIC/SECRET哈希与`v5-core-lock`标签。

## v4与v5更新

- v4-r1已经冻结，开发证据提交为`096e64a`，冻结提交为`5bd2a3a`，标签为`freeze-v4-r1`；
- v4的18例预测在标签揭晓前完成SHA-256锁定，揭晓后结果完整保留；该集合对v5只具有开发身份；
- v5-pcg-r1采用“Pattern精英 + 强反事实 + V4 Top-5外救援必要性”的离散一致性门控，不拟合连续权重；
- V5完整开发运行前的单份干净表smoke暴露了“已在V4 Top-5内仍报警”的问题，已通过公开修订1限制为只救援Top-5外候选；
- V3/V4核心源码哈希已回归验证，v5实现位于独立模块，不覆盖历史冻结模型；
- 62项短测试通过；V5完整开发运行已完成：18例合成Top-5为100%、MRR为0.9722，48份干净表联合确认报警率为2.08%，但Enron 30事件MRR为0.3418，低于0.363门槛且相对V4的配对MRR差为-0.0407（95%区间[-0.1009,-0.0046]）；
- 因此V5被正式否决，不生成`frozen_config_v5.json`，也不开发V5-R2。多源Enron事件的审计口径修正及原始结果哈希见 `research\V5_AUDIT_CORRECTION_AND_DECISION.md`。

## V5.2实施状态

- 已建立独立`formulaguard/v52.py`，不修改冻结V4源码；
- 固定输出为V4完整核心排序、可空的单一救援位以及最多六格的审查集合；
- V5.2-A为基础双通道，B增加唯一性/优势门槛，C再增加双来源修复一致性；
- 已完成18合成、30 Enron、36平衡红队、48干净表上的A/B/C三轮完整开发与安全回归，按预登记规则选择V5.2-B：A/B均有7个新增救援命中且干净表率同为2.08%，B运行时间较短；C仅3个新增命中，未被选择；
- V5.2-B冻结配置已提交为`870bcb8`并标记`v5.2-lock`；V4冻结源码不变；
- 已由独立出题人制作15例公开工作簿，标签在联合预测锁成功后才释放；锁定元数据证明预测阶段`label_inputs`为空，记录15个工作簿、配置、源码和预测文件SHA-256；
- 揭盲评分完成：V4在15事件独立案例集上Top-1为0.5333、Top-3/Top-5均为0.8667、MRR为0.6726、EXAM为0.0758；V5.2-B核心Top-5逐例等于V4，额外审查位激活2次且2次均为V4 Top-5外真实源、无正确例外暴露；
- 新增独立结果审计脚本与结果说明，短测试共73项通过。小样本边界、随机参照边界和不得声称优于实际基线的限制见`research\V4_V52_INDEPENDENT_RESULTS.md`。

当前不启动V6：独立集已揭盲，继续根据其标签调主排序会污染最强证据。后续优先论文、图表和答辩；未来若做新主模型，必须另立开发/独立集与预登记机制假设。

上句是 100 例刚揭盲时的阶段性停止决定，现已由下面的 V6 新协议取代；
100 例仍严格只作历史失败分析，未成为 V6 开发或验证数据。

## 下一步与分工

V5开发运行已停止。V5.2三轮和之后的独立验证默认使用24个CPU工作进程；GPU不参与，因为方法由公式解析、图遍历和大量小型反事实重算组成，不是适合GPU的大矩阵训练。

## 2026-08-29：V5-Core R2-R1当前状态

- 新核心为候选无关Source排名、制度条件残差RCR、匹配公式安慰剂DCF和跨工作簿干净空模型WCN；它不使用V4主排序，也不依赖正确修复必须进入候选池；
- 480错误+360干净回顾性重跑完成：R2-R1 Source Top-5 99.58%、MRR 0.9390；Full Top-5 99.58%、MRR 0.9453；最弱类型Top-5 97.50%；
- 原始预登记回执仍为失败，唯一失败项是Full相对Source的安全上调只覆盖2类。复核确认该实现并不衡量模型相对基线的改进覆盖面；原失败回执永久保留，解释另立文档，不修改历史结果；
- 去除边界保护的消融仍比完整模型高0.0193 MRR，因此已建立100例历史集和Enron 30事件的一次性压力安全门；截至本状态记录，压力大型实验尚未运行；
- 压力运行、完整性审计、安全回执、冻结脚本以及第三方780例无标签预测锁/评分协议均已实现。任何冻结必须先取得`pressure_safety_passed=true`，且冻结配置会保存360个干净控制形成的经验空分布；
- 第三方确认集规定600个错误事件和180个干净/拒绝控制，预测前公开清单只有`instance_id,workbook`，标签、干净/错误身份和错误类型必须在V4/V4.3/R2-Source/R2-Full完整排名锁定后释放；
- 当前下一条大型命令是`run_v5_core_r2_r1_pressure.cmd`，统一使用24进程。其结果只决定是否允许进入新的独立确认，绝不成为第二轮调参数据。

## 2026-08-19：100例扩展验证（覆盖并补充原15例结论）

- 新公开包包含`independent_001`至`independent_100`；其中前15例已经在早期Cohort-1揭盲，新增的独立证据为Cohort-2的85例（`016`至`100`），两者不得混称为100例全新盲测；
- 预测锁先行成功：100个工作簿、24个进程、`label_inputs=[]`、V5.2-B核心排序100%等同V4；
- Batch-2私密压缩包和完整100例标签/例外CSV均精确匹配预测前承诺的SHA-256；标签与例外表各100个唯一ID，六类错误覆盖17/17/17/17/16/16；
- Cohort-2结果：V4 Top-1为17.65%，Top-3为32.94%，Top-5为56.47%，MRR为0.3311，EXAM为0.0851，精确修复率为50.59%；Top-5的95% bootstrap区间为[45.88%，67.06%]；
- 合并100例仅作次要汇总（Top-5 61.00%、MRR 0.3823），不得掩盖Cohort-2明显更难的结果；
- 失败归因：新增集中的`function_replacement`和`range_boundary`均为0/14 Top-5，而reference/copy/operator类型均在85%以上；现有V4不具备可声称的六类稳健泛化能力；
- V5.2-B继续只作为安全辅助审查位：Cohort-2救援12次、正确7次、精确率58.33%、新增Review@6命中7次；它不改变V4 Top-5，也不得作为主性能提升；
- 100例已经揭盲，不得用于主排序调参或直接开发V6。完整证据、统计区间、失败边界见`research\V4_V52_INDEPENDENT_100_RESULTS.md`。

## 2026-08-25：V6 实施状态

- 已将新增 85 例的函数替换、范围边界系统性失败写成只读假设审计，明确标注不得用于 V6 选择；
- 方法规格已预注册于 `research\V6_METHOD_SPEC.md`；V6 复用冻结 V4 完整排名和能量计算，不修改 V4/V5.2 源码；
- 已实现相对 AST 公式族一致性、范围端点一致性、25 候选配额、局部反事实 D、匹配替代编辑 IRG、全局副作用、唯一性和单一稳定提升；
- V6-A/B/C 与八项固定消融已进入独立模块；消融按 A/B/C 分别生成并只与同变体比较；公开接口不接受源单元格、错误类型、正确公式或标签；
- 新建开发 1,200、内部验证 360、干净 240、红队 360 的生成和质量/泄漏审计代码；每层记录生成器、清单和完成凭据哈希；
- 第三方最终 600 例打包器已改为只验证项目外真实工作簿对，禁止在 final 模式调用内部生成器；要求 480 程序化、120 真实半人工、至少 30 个未见模板及逐例复核记录；
- 已实现按工作簿原子分片、24 进程、输入/源码/提交哈希验证和 `--resume`；完成标记前逐分片检查全排名无遗漏、无重复；标签只在完整预测落盘后由独立评分脚本读取；
- 首次 V6-A 调用在任何模型预测开始前被数据硬门槛拦截：开发集 15/1,200、红队 8/360 的相邻随机 B 值恰好相等，导致引用平移后汇总值不变；完整原因、旧清单哈希和修复边界保存在 `research\V6_DATA_GATE_FAILURE_R1.md`，无案例被删除或改标签；
- 生成器现以与错误标签无关的种子仿射序列保证相邻 B 值不同；新增回归逐一验证开发/红队全部 520 个引用平移与复制偏移案例均真实改变预登记汇总值；96 项全项目测试通过；旧 V4/V5.2 和 V6 模型源码哈希不变；
- 修复提交后重新运行的 r4 smoke 已通过全部 12 项工程审计，包括 24 例、六类覆盖、Candidate Coverage@25、6 份干净特殊公式、完整排名、续跑和公式安全导出；该结果只用于工程验收；
- 修复后的正式内部数据已完整重建并在模型预测前再次审计：开发 1,200/1,200 有效、红队 360/360 有效、干净控制 240/240 接受，跨划分泄漏检查通过；固定清单哈希见 `research\V6_DATASETS_R1_READINESS.json`；
- `research\V6_IMPLEMENTATION_READINESS.json` 的 12 项实施就绪检查全部通过，V6-A 大型开发轮次可以启动；V6-B、V6-C 仍须按预登记顺序在各自上一轮审计完成后运行；
- 在加入合法特殊公式和多公式块后的困难探针中，V4 对函数替换的源排名为 39，V6-C 提升至 3；该数字只证明生成器能产生非平凡案例，不作为论文结论；
- 尚未运行 1,200 例 A/B/C 大型开发、360 例一次性内部验证或第三方 600 例最终独立验证，因此 V6 目前**没有冻结，也不能声称优于 V4**。

## 2026-08-26：V6-A 开发轮次审计

- V6-A 已按预登记以 24 个工作进程完成 1,200 个开发事件、360 个安全红队事件、240 份干净控制和 Enron 回顾性评测；原始分片数量、全排名完整性、事件—方法唯一键、指标恒等式、汇总复算和标签隔离均通过独立审计；
- 开发集宏平均 Top-5 从 V4 的 43.67% 提升至 87.92%，MRR 从 0.2986 提升至 0.4245；V6-A 新增 531 个 Top-5 命中且未丢失原 Top-5 命中；
- 红队宏平均 Top-5 从 40.00% 提升至 68.89%，MRR 从 0.2345 提升至 0.3119，但出现 4 个原 Top-5 命中丢失，说明困难歧义条件下仍存在排序副作用；
- V6-A 仅包含公式族一致性，因此范围边界仍是系统弱项：开发集 Top-5 为 27.50%，红队为 1.67%；该结果支持继续验证预登记的 V6-B 范围边界一致性机制；
- 干净控制误报为 40/240（16.67%），超过 10% 门槛；40 个误报全部来自 40 份合法 `exception` 结构，其余五类结构均为 0，表明合法交替 MAX/MIN 等特殊公式会被错误视为族异常；
- 首轮脚本误继承旧 Enron 的20事件测试分区，而V6计划要求30个既有可评测事件全部作为回顾性安全检查；该协议接线错误已在不修改V4/V6模型源码的前提下修正。原20事件结果保留为本地历史快照，30事件校正结果使用25份工作簿，V4与V6-A的Top-5均为50%；仅一例由第184名降至第185名，使MRR变化 `-0.00000098`，故严格不下降门槛仍失败；修正、清单哈希和复用回执见 `research/V6_ENRON_INVENTORY_CORRECTION.md`；
- 独立单工作簿机制探针显示，现有固定 A/B/C 均会在同一合法例外表上触发提升；它不是总体统计，但预警 B/C 可能仍无法消除该类误报，因此不得提前假设 C 必然通过安全门槛；
- V6-A 的 `round_passed=false`，不得冻结或用于正式结论。下一步仍按预登记顺序运行未经修改的 V6-B、V6-C；A/B/C 全部完成前禁止选择版本，固定八项消融将在一次性内部验证中运行。
- 可复算证据位于 `research/V6_A_DATA_QUALITY_AUDIT.json`、`research/V6_A_RESULTS_AUDIT.md` 和 `scripts/analyze_v6_round_results.py`。
- 当前短测试为97项且全部通过；V6 Enron与内部预测调度已改为按实际完成顺序回报，避免大型早序号工作簿阻塞进度显示；两次误以28进程启动的V6-B均被判为无效且未进入研究结果，相关本地缓存已按用户要求删除；正式A/B/C轮次统一使用24个工作进程。

## 2026-08-26：V6-B 开发轮次审计

- V6-B 已按预登记以24个工作进程完成1,200个开发事件、360个安全红队事件、240份干净控制和30个Enron回顾性事件；原始分片、全排名、指标恒等式、标签隔离、清单哈希和汇总复算均通过独立审计；
- 加入BSS后，开发集六类型宏平均Top-5达到100%，MRR为0.4540；相对V4新增676个Top-5命中且没有丢失，其中范围边界开发Top-5由V6-A的27.50%升至100%；
- 红队宏平均Top-5达到85.28%，MRR为0.3565；范围边界Top-5由V6-A的1.67%升至100%，证明BSS确实补上了预登记的范围语义缺口；但绝对引用仅35% Top-5，且相对V4仍丢失3个Top-5命中；
- 干净控制误报仍为40/240（16.67%），且仍全部集中在合法`exception`公式族；因此BSS提高范围错误定位，但没有解决合法特殊公式误报；
- Enron 30事件Top-5与V4均为50%；仍只有同一事件下降一个名次，MRR差为`-0.00000098`，所以严格的Enron不下降门槛继续失败；
- V6-B的`round_passed=false`，不得冻结。下一步运行未修改的预登记V6-C，检验双来源、唯一性、副作用与并列拒绝是否能降低误报和有害提升；
- 可复算证据位于`research/V6_B_DATA_QUALITY_AUDIT.json`、`research/V6_B_RESULTS_AUDIT.md`和`results/v6_development_b/v6_round_audit.json`。

## 2026-08-26：V6-C 与三轮开发比较

- V6-C 已以24个工作进程完成与A/B完全相同的1,200开发、360红队、240干净控制和30 Enron回顾性协议；独立复算确认所有分片、完整排名、指标恒等式、标签隔离和输入哈希有效；
- V6-C开发宏平均Top-5为90.58%、MRR为0.4388，红队宏平均Top-5为85.28%、MRR为0.3565；函数替换与范围边界均保持较强表现，但开发集绝对引用Top-5仅43.50%；
- C相对B没有降低干净控制误报，三种变体均为40/240（16.67%）；C在Enron 30事件上与V4逐事件排名完全一致，消除了A/B的单事件一名退化；
- A/B/C开发比较证明BSS对范围边界具有明确机制贡献：B相对A的开发范围边界Top-5增加72.50个百分点、红队宏Top-5增加16.39个百分点；C安全门控未解决合法例外公式误报，且相对B使开发宏Top-5下降9.42个百分点；
- 三轮开发比较只作机制诊断，不选择模型。静态审计发现选择脚本曾额外加入未在`V6_METHOD_SPEC.md`登记的`development_round_passed`门槛；在360例锁定验证运行和读标签之前已移除该选择门槛，同时完整保留为诊断字段，V6模型、数据、阈值和既有预测均未改变；
- 下一步按原预登记对固定A/B/C和八项匹配消融执行一次性360例锁定内部验证；仅锁定验证门槛可以授权冻结。修正边界见`research/V6_LOCKED_SELECTION_WIRING_CORRECTION.md`，三轮比较见`research/V6_ABC_DEVELOPMENT_DECISION.md`。

## 2026-08-26：360例内部验证预承诺

- 固定生成器已建立360个验证事件及对应360份错误、360份正确工作簿；标签感知的数据质量审计与开发/红队/干净层跨划分泄漏审计作为独立准备阶段完成，全部通过；
- 在任何验证预测生成前，`research/V6_VALIDATION_PRECOMMIT.json`已锁定8个公开清单/审计文件、720份工作簿聚合清单、内部标签文件、V4/V6源码和方法规格的SHA-256；预承诺时`results/v6_validation_locked/predictions`不存在；
- 验证运行器已改为预测前只验证公开文件、工作簿和模型哈希，不再在预测命令内构建数据或读取标签做质量审计；只有360份完整排名全部落盘且完整性审计通过后，才验证标签哈希并启动评分；
- 该证据只证明内部验证的顺序隔离与防篡改，不冒充第三方盲测；最终600例仍必须由外部出题者保管标签。完整边界见`research/V6_VALIDATION_LABEL_SEPARATION.md`。

## 2026-08-27：V6锁定内部验证与停止决定

- A/B/C及八项匹配消融已在同一批360个平衡验证事件上一次性完成预测；360个原子分片、10,080个事件—方法结果、全排名、24进程回执、公开/秘密预提交哈希和标签后读顺序均通过独立复算；
- V6-B是内部合成验证上的最强定位器：六类型宏平均Top-5与最弱类型Top-5均为100%，MRR为0.3177，精确修复率为100%；相对V4的配对MRR差为+0.2171，95% bootstrap区间为`[0.2083, 0.2258]`；
- 上述提升不能直接解释为真实泛化优势：三种变体的`semantics_only`消融在360例上均达到Top-1和MRR 100%，说明验证生成器与V6语义规律高度对齐；预测元数据确认没有读取标签，因此这是合成对齐风险，而不是标签泄漏证据；
- A/B/C在240份正确控制表上均报警40次，误报率16.67%，超过预注册10%上限；40次全部集中在合法`exception`结构，其余五类结构为0，表明模型系统性地把合法多公式族/交替公式误判为异常；
- V6-A/B还因Enron同一尾部事件由第184名降至第185名而违反严格不下降门槛；V6-C虽保持Enron不退化，但最弱类型Top-5仅8.33%，并被预注册语义消融超过0.02；
- 选择回执为`selected_variant=null`、`freeze_allowed=false`。`run_v6_freeze.cmd`已验证会在任何写入、提交或打标签前拒绝执行；V6不冻结，不运行性能和第三方600例，也不在本协议内开启V7；
- V4-R1继续作为冻结主模型。V6保留为机制实验：BSS补齐范围边界是正结果，合法例外误报和合成语义对齐是关键负结果。完整复算见`research/V6_VALIDATION_AUDIT.json`与`research/V6_VALIDATION_RESULTS.md`。

## 2026-08-27：正式版本体系修正

- 论文和新公共接口将旧V5改称V4.1-PCG、旧V5.2-B改称V4.2-Review-B、旧V6改称V4.3-Semantic；这三项方法均直接复用V4主排序或保持V4 Top-5，属于V4.x机制研究；
- 真正的V5-Core预留给候选中心、多证据责任的主排序核心重构，最低要求是不调用`v4_scores`取得最终基础排名；
- 新增`formulaguard/v4x.py`提供V4.1/V4.2/V4.3别名，并在输出中同时记录正式版本号和历史版本号；旧调用仍可复现实验；
- 旧源码、冻结配置、方法规格、Git标签、结果目录和预测锁不批量改写，因为其中版本字符串已进入SHA-256和复现证据；
- 完整映射和论文写法见`research/VERSION_LINEAGE_AND_NAMING_POLICY.md`。

## 2026-08-28：V5-Core R2 双空模型机制阶段

- 新增`formulaguard/v5_core_r2.py`；R2不调用V4排序，先以候选无关的制度条件残差、行为/图残差、传播潜势和异常祖先惩罚生成完整Source排名，再只在观测不确定集合内使用匹配公式安慰剂干预重排；
- R2固定输出三项互补机制：RCR用于制度条件残差，DCF要求局部净改善与有向下游恢复同时存在，WCN用开发干净工作簿防止每张无错表被强行判错；
- 预测流水线一次输出7项机制消融、候选移除25%/50%/100%、三种WCN统计量、完整排名、失败清单、CSV汇总、哈希回执与阶段门槛；预测分片完成并核验后才读取已揭晓标签；
- 阶段A在24错误+24干净的工程数据上通过：R2-Source Top-5为100%、MRR为0.8958；R2-Full Top-5/MRR为100%；全部候选移除时逐事件退回Source排序；预登记WCN选择RCR，留一法干净误报为4.17%、错误报警召回为100%；
- 该小样本仍与R2生成体系同源，明确仅为工程入口，不能用于论文主结论或“优于V4”的声明；当前唯一授权的大型下一步为480错误+360干净的24进程回顾性实验，之后才决定是否值得做85例与Enron压力测试、允许一次有根因记录的修正或停止R2。

## 2026-08-29：V5-Core R2回顾性失败与R2-R1唯一修正

- 原R2已完成480错误+360干净的回顾性开发评测：Source宏Top-5为93.33%、MRR为0.8914；Full宏Top-5为93.13%、MRR为0.8938；但函数替换Source Top-5仅63.75%，因此未通过冻结；
- 固定消融发现无边界保护MRR为0.9460、无远距离角色复现的函数替换Top-5为91.25%，说明原例外保护存在系统性过度保护，不能被包装成有效贡献；
- 直接取消保护不可采用：小型错误/干净表筛选显示WCN报警召回降至0%至37.5%，因为干净表合法特殊公式会变成高残差；
- 已登记唯一修正R2-R1：自适应例外释放只在传播潜势相对匹配对照显著异常时解除结构保护；定位排序使用释放后的残差，WCN保留受保护的全表保守视图；DCF仅重排显著候选且严格限制为Source前12名；
- 已完成80例函数替换定向回归：R2-R1的Source/Full Top-5均为100%、MRR均为0.8688，Full没有任何相对Source的排名下降。该切片仅授权重跑，不能作为论文结论；
- 新的24进程完整命令是`run_v5_core_r2_r1_retrospective.cmd --workers 24`，结果写入`results\v5_core_r2_r1_retrospective_full`。若这一轮仍失败，则停止R2，不开启R2-R2或继续在已揭晓数据上刷分。

## 2026-08-29：R2-R1完整回顾结果与独立确认前置协议

- R2-R1已完成480错误+360干净的完整回顾性重跑：Source Top-5为99.58%、MRR为0.9390，Full Top-5为99.58%、MRR为0.9453，最弱类型Top-5为97.50%；候选干预只安全改善6个事件，没有制造Top-5回退；
- 原预登记审计仍如实记为失败，唯一失败项是“Full相对Source的改善覆盖至少四种错误类型”。它衡量的是候选重排的增量广度，不是否定候选无关Source定位器的六类型覆盖；其余类型已接近饱和，因此不得通过改写门槛或继续调权重把该记录变成通过；
- 固定消融`no_boundary`的MRR高于Full约0.0193，仍低于预登记0.02拒绝线，但作为方法解释警告保留，论文不得把边界保护写成已获正向消融证明；
- 已新增历史100例和Enron 30事件的24进程回顾性压力协议。只有压力安全回执通过，R2-R1才有资格冻结并进入一批全新的第三方独立确认；这些已揭晓数据永远不改称独立验证，也不得再用于调参；
- 第三方780例确认集现有独立打包审计工具：在不运行定位器的前提下检查600错误/180干净的精确粒度、原始—错误工作簿一致性、静默数值和下游传播、字节唯一性、开发集公式变换与图/公式签名零重叠、许可/脱敏、真实结构和人工构造数量，再生成PUBLIC/SECRET及预提交哈希；
- 当前唯一需要用户运行的大型任务是`run_v5_core_r2_r1_pressure.cmd`。压力安全未通过时冻结命令会拒绝，第三方确认集也不得启动模型预测。
