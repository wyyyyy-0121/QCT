# FormulaGuard 项目状态

更新时间：2026-08-10

## 已完成

- 核心公式解析、依赖图、局部重算和 GIR 定位；
- 六类静默错误候选修复及可审计候选元数据；
- 传播影响的有界加性评分、区域/聚合一致性和链式依赖检查；
- 合成基准、标签隔离、主对比、五项消融和 bootstrap 统计；
- 干净表约束阈值、预登记敏感性方案和 quick 冻结配置工具；
- full 对冻结配置、代码哈希、Git提交和工作区状态的强制验证；
- Enron properties 清点、旧 `.xls` 转换清单和真实语料评测入口；
- 结果报告、图表、Excel工作簿、公式错误扫描和完成度审计；
- 工作簿级多进程调度：默认使用约 75% 的逻辑处理器，受限环境自动回退串行；
- quick/full 运行前后 Git 提交锁，拒绝脏工作区和实验途中版本变化；
- 17项单元/协议测试全部通过；
- 当前版本完整 smoke 已通过严格审计。

## 当前 smoke 诊断结果

- 12/12 错误实例有效，六类错误和浅/中/深传播均覆盖；
- FormulaGuard MRR 0.867，最强无真值基线 MRR 0.588；
- 配对 MRR 差值 0.279，smoke bootstrap 95% CI 为 `[0.071, 0.492]`；
- Candidate Coverage@15 为 100%，修复 Top-1 为 83.3%；
- 阈值校准错误召回率 83.3%，两本干净表报警率 0%；
- 结果工作簿公式错误扫描为 0，完成度审计通过。

这些数字来自很小的开发集，只说明阶段A/B工程目标完成，不能写入论文主结论，也不构成获奖保证。运算符类仍是已知困难案例；完整方法与部分消融在 smoke 上并列，必须由 quick 检验各分量贡献。

## 首次 quick 诊断（并行化前版本）

- 48/48 错误实例有效，4个模板族、6类错误和3档传播深度均覆盖；
- FormulaGuard MRR 0.8655、Top-1 83.3%、Top-5 97.9%；
- 最强无真值基线 Excel-like MRR 0.5880；配对差值 0.2775，bootstrap 95% CI 为 `[0.1729, 0.3899]`；
- Candidate Coverage@15 为 100%，修复 Top-1 为 83.3%；
- 8本干净合成表报警率 0%，阈值校准错误召回率 83.3%；
- 4个模板族均不落后于最强基线；仅有1个实例的排名落后；
- M3 运算符错误仍是主要弱点：Top-1 为0，Top-5 为87.5%；
- 完整方法与 `ablate_influence`、`ablate_intervention` 的定位 MRR 并列，但去掉干预后修复 Top-1 从83.3%降至50.0%。

该次 quick 达到数值冻结线，但运行期间 Git 历史被重置并重新提交，且随后加入了不改变算法得分的多进程调度。旧 quick 的64个源文件哈希与当时工作区完全一致，但其提交号和当前代码版本均不再适合正式冻结。因此必须在提交并行化改动后重跑 quick；新旧结果应逐项一致，仅总耗时和并发调度不同。

## 下一步大型实验

用户只需运行：

```bat
cd /d D:\code\QCT
run_pipeline.cmd quick -Ablations -WithSensitivity
```

运行完成后只需告知 Codex。Codex 将直接读取 `results\quick`，执行冻结资格审计；达到预登记门槛后生成 `results\quick\frozen_config.json`。在该文件生成前，full 流水线会主动拒绝运行。
