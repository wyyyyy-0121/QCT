# FormulaGuard 项目状态

更新时间：2026-08-18

## 研究目标

在没有正确输出真值、错误公式仍返回正常数值时，利用公式依赖图、候选修复和反事实干预，定位引发下游传播的静默源公式错误。项目按高中生竞赛的本科课程设计水平控制范围，内部目标是形成具备银奖竞争力的完整证据链。

## 已完成

- A1公式解析、归一化、依赖图、下游传播路径和可重算工作簿模型；
- 六类候选修复、候选质量元数据和Top-15编辑族配额；
- 公式、图、行为、内部一致性约束和图干预责任分数GIR；
- Random、Excel-like、Pattern、Graph、Behavior、ExceLint-like、WARDER-like及SFL-Oracle参照；
- 五项消融、敏感性、干净表报警、性能、失败案例和报告流水线；
- 标签物理隔离、数据验证、Git锁、冻结配置、文件哈希和完成度审计；
- 工作簿级多进程调度，默认使用约75%的逻辑处理器；
- 超长结果工作簿采用有界预览，完整行仍保留在XLSX中；候选公式按文本写入；
- Enron清点、`.xls`转换和外部评测入口代码。

## v1 已完成但仅作历史基线

冻结版本：提交 `6a91204e1137532301f11356e63fa5fbf519be76`，标签 `freeze-v1`。

- full：864/864实例有效；
- FormulaGuard MRR 0.6899，最强无真值基线0.4696；
- 配对MRR差值约+0.2203，bootstrap 95%区间为正；
- 干净表报警率0%；
- 完成度审计和结果工作簿公式扫描通过。

限制：v1六个测试族的核心依赖结构相同，因此这些数字不能作为最终“跨结构泛化”结论，也不再为导出故障重跑。

## v2 已完成的工程与诊断证据

### 多结构基准

v2实现了层级树、横向链、多表级联、矩阵汇聚、纵向滚动和分叉—汇合六类依赖拓扑。结构审计直接从XLSX重建图，不依赖生成器标签。

困难条件探针 `v2_sparse`：

- 36/36实例有效；
- 六类错误各6例，浅/中/深各12例；
- 六个声明拓扑和六个实算结构签名均不同；
- 覆盖单表、多表、跨表和非跨表结构；
- Candidate Coverage@15为100%；
- FormulaGuard MRR 0.9253，最强无真值基线ExceLint-like为0.6560；
- 配对差值+0.2693，bootstrap 95%区间 `[0.1290, 0.4170]`；
- 正确修复Top-1为100%。

以上36例是定向困难探针，不替代预注册quick和冻结full。

### v2 smoke

- 12/12实例有效，六类错误和三档深度完整；
- 两个开发族的声明拓扑和实算结构签名均不同；
- Candidate Coverage@15和正确修复Top-1均为100%；
- 23项单元测试通过；
- 报告、演示、结果工作簿、公式扫描和完成度审计通过。

smoke中Pattern和WARDER-like优于FormulaGuard，原因是两个可见版本的复制规律很明显；该结果用于确认简单条件下基线确实有竞争力，不用于论文主结论。

### 性能修复

行为异常和内部约束已经共享同一次工作簿求值，消除了每个候选的重复重算，并增加回归测试保证以后不会恢复为双重求值。GPU不参与，因为当前算法是公式解析、图遍历和大量小规模分支求值，适合CPU多进程而非神经网络矩阵运算。

## v2 冻结正式结果

冻结提交：`672a62523ff9c0a36e2b9712bf7a9be3746c8f61`，标签 `freeze-v2`。

- full 864/864 实例有效；MRR 0.9452，Top-1 0.9144，Top-5 0.9896；
- 最强无真值基线 WARDER-like MRR 0.7788，配对差值 +0.1664，95% CI `[0.1422, 0.1899]`；
- 干净表报警率 16.7%，Coverage@15 100%，正确修复 99.4%；
- LibreOffice 交叉验证 906/906 公式值一致；
- 局限：`ablate_graph` MRR 0.9682，高于完整模型约 0.023，因此不得宣称固定局部图异常项有效。

## v3 当前进度

- 已预注册结构自适应反事实责任模型，v2 证据和目录保持只读隔离；
- 已实现局部结构可靠度、净反事实改善、副作用惩罚、候选级下游恢复与路径责任；
- 已增加 Source-before-descendants、Source-first-in-causal-cone、Path Coverage、Repair Safety 和证据强度报警；
- 已建立全新开发、验证和六个测试族，并增加独立控制链与无关合法公式区；
- v3 smoke：12/12 有效，六类错误和三档深度完整，一键流水线与严格审计通过；
- v3 smoke 只作为工程验收：v3 MRR/Top-1/修复率均为 100%，但 v2 同样为 100%，不能作为 v3 优势结论；
- 六测试拓扑探针：36/36 有效，六个声明拓扑和六个实算签名均不同；
- 首轮 v3 quick：48/48 有效，MRR 0.9592、Top-1 0.9583、修复率 0.9583、干净表报警率 0%；相对最强无真值基线的 MRR 差值为 +0.3586，95% CI `[0.2535, 0.4664]`；
- 首轮 quick 同时暴露出固定截断缺陷：两个第 51 位源公式未进入前 50 个干预对象，另有四例传播路径因只保存前 10 个终点而漏报；因此首轮冻结已经作废，完整证据保存在 `results/v3_quick_pre_revision`；
- 已按预注册允许的唯一一次 quick 后修订，将典型工作簿的干预上限提高到 100，并输出全部可达检查/结构终点；未改权重、候选规则、Top-15、阈值目标或数据生成器；
- 修订后 29 项单元与回归测试通过；v3 smoke 12/12 有效，MRR、Top-1、Top-5、路径覆盖和修复安全性均为 100%，结果工作簿公式错误扫描为空。

## 当前未完成

- 运行V5.2-A/B/C三轮完整开发与安全回归，按固定规则选择或拒绝；
- 由老师/同学制作15例项目外保管标签的独立验证案例；
- 用冻结V4与最终V5.2完成同集联合预测锁、揭盲评分；
- 以V4主模型、V5负结果和V5.2安全修正完成论文结果章节；
- 正式论文图表、三分钟演示和答辩材料。

## v4与v5更新

- v4-r1已经冻结，开发证据提交为`096e64a`，冻结提交为`5bd2a3a`，标签为`freeze-v4-r1`；
- v4的18例预测在标签揭晓前完成SHA-256锁定，揭晓后结果完整保留；该集合对v5只具有开发身份；
- v5-pcg-r1采用“Pattern精英 + 强反事实 + V4 Top-5外救援必要性”的离散一致性门控，不拟合连续权重；
- V5完整开发运行前的单份干净表smoke暴露了“已在V4 Top-5内仍报警”的问题，已通过公开修订1限制为只救援Top-5外候选；
- V3/V4核心源码哈希已回归验证，v5实现位于独立模块，不覆盖历史冻结模型；
- 62项短测试通过；V5完整开发运行已完成：18例合成Top-5为100%、MRR为0.9722，48份干净表联合确认报警率为2.08%，但Enron 30事件MRR为0.3418，低于0.363门槛且相对V4的配对MRR差为-0.0407（95%区间[-0.1009,-0.0046]）；
- 因此V5被正式否决，不生成`frozen_config_v5.json`，也不开发V5-R2。多源Enron事件的审计口径修正及原始结果哈希见 `research\V5_AUDIT_CORRECTION_AND_DECISION.md`。

## V5.2实施状态

- 已建立独立`formulaguard/v52.py`，不修改冻结V4源码；
- 固定输出为V4完整核心排序、可空的单一救援位以及最多六格的审查集合；
- V5.2-A为基础双通道，B增加唯一性/优势门槛，C再增加双来源修复一致性；
- 已建立18合成、30 Enron、36平衡红队、48干净表的逐轮运行与审计命令，默认24进程；
- 已建立15例公开清单严格校验、V4/V5.2联合预测锁和项目外标签揭盲评分；
- 新增测试后仓库共72项短测试通过，冻结V4源文件哈希保持一致；
- 当前尚未运行三轮完整大型实验，也尚无老师/同学制作的15例真实独立集，因此不存在V5.2冻结配置或独立结果。

下一步先运行`run_v52_development.cmd a 24`。A轮完成后仍继续B、C，不根据单轮结果临时改阈值。独立出题人可以同时按照`research\V4_V52_INDEPENDENT_HANDOFF.md`准备15例工作簿，但答案在联合预测锁成功前不得交给模型开发者。

## 下一步与分工

V5开发运行已停止。V5.2三轮和之后的独立验证默认使用24个CPU工作进程；GPU不参与，因为方法由公式解析、图遍历和大量小型反事实重算组成，不是适合GPU的大矩阵训练。
