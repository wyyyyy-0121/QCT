# FormulaGuard-v3 第一次 quick 数据质量审计

审计日期：2026-08-11
代码提交：`4e79825e4385b31645d6145703b0bc9c753fd4f0`
原始结果备份：`results/v3_quick_pre_revision`

## 结论

第一次 quick 的数据与证据链完整，所有预注册脚本门槛均通过，但不批准直接进入 full。原因是 quick 揭示了一个可明确归因的 Top-50 反事实截断缺陷，而且 v3 与 v2 的 48 个实例排名完全相同，尚未证明新版本带来定位增益。

## 数据质量

- 48 个有效实例，开发/验证各 24；六类错误各 8；深/中/浅各 16；
- 624 行原始结果等于 `48×13`，624 个 `(instance_id, method)` 组合全部唯一；
- v3 48 行均有合法排名，无缺失或越界；
- 四个模板族与四个实算结构签名完整；
- 标签物理隔离、Git 锁、结果工作簿公式扫描和完成度审计均通过。

## 主要结果

- v3：MRR 0.9592，Top-1/Top-5 95.83%，Coverage@15 100%，修复准确率 95.83%；
- 最强无真值基线 WARDER-like：MRR 0.6005；配对差 +0.3586，95% CI `[0.2535, 0.4664]`；
- v3 与冻结 v2：48 例排名完全相同，MRR 差 0，95% CI `[0,0]`；
- 三项 v3 消融与完整模型排名完全相同；
- v3 证据强度阈值 0.31546，错误召回 95.83%，8 个干净工作簿报警率 0%；同数据上的 v2 也可校准到 0% 报警，因此 quick 尚未证明 v3 的误报优势；
- Source-first 95.83%，Path Coverage 87.5%，Repair Safety 100%。

## 缺陷定位

两例失败均为 `cashflow_lattice_v1` 的浅层错误，公式数 54，源排名均为 51。正确修复已在 Candidate Top-15 内，但实现只干预先验 Top-50，因此源单元格没有候选输出。将 v3 干预上限提高到 100 的诊断中，两例都升至第 1，净改善均为 0.47036，候选修复完全正确。

其余四例 Path Coverage 失败来自输出只保留前 10 条终点路径；目标汇点在图上可达，但未被序列化。这是解释完整性问题，不是依赖图不可达。

## 唯一修订与复验要求

执行预注册允许的唯一一次 quick 后修订：v3 在不超过 100 个公式时全覆盖干预，并输出全部可达终点路径。权重、候选规则、阈值目标和数据不变。修改后必须重新运行完整 quick；只有新冻结再次通过门槛，才能运行 full。
