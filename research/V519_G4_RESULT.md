# V519 G4 result

The fixed milestone passed all 12 distinct 13-cell workbooks on both backends
(24 comparisons): product/ratio/weighted x true pair/chain x two engineering seeds.
Every unchanged-budget V518 run refused with candidate_dependency/budget_exceeded;
every V519 run completed a correct unique group and passed independent replay.

Actual V516 enumeration checked all 8192 original assignments per comparison,
196608 states in total. V519 actions and complete candidate/ranking identities
agreed with that oracle. Unsafe groups and ranking/candidate changes: zero.

Evidence is in V519_G4_V1: raw synthetic inputs, diagnoses, oracle audits,
per-case measurements and hashes. Timing was collected with concurrent workers
and is not the later serial benchmark. Backend repetition does not increase the
12-workbook independent-input count. Initial-preparation counting and component
node units differ from the V518 historical audit convention as predeclared.
