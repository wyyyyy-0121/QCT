# FormulaGuard model-discovery 研究选择日志

本文件是追加式日志。每条记录必须写明观察到的证据、可作出的决定、没有作出的
决定以及是否读取了受保护标签。历史结果不能被新条目改写。

## D0001：研究线重置

- 日期：2026-08-31
- 依据：V5-Core、R2、PSL 的失败审计；Phase 0 数据分组审计。
- 观察：合成数据上的高分与生成机制重合；Enron 的自然错误结果没有显示稳定提升；
  公开压力和历史数据的事件数高于独立结构组数。
- 决定：停止继续调旧权重，启动 `model-discovery`；`V4-R1` 继续冻结主模型。
- 没有决定：没有选择区域、图、语义、反事实或学习排序器作为新核心；没有创建
  另一个 reset 版本；没有恢复任何旧 V5 名称。
- 受保护数据：未读取旧或新 `240+120` 的内容、标签或承诺以外的字段。
- 可复核依据：`research/V5_CORE_FIRST_PRINCIPLES_DISCOVERY_PLAN.md`、
  `results/core_reset_b_phase0/data_audit.json`。

## D0002：数据角色重分类

- 日期：2026-08-31
- 依据：公开压力清单、Enron 清单、历史 100 分组和现有语料注册表。
- 观察：Enron 是当前主要自然错误来源；Info1 只有 2 个纳入事件；Modified EUSES
  和 Integer 主要是注入故障；FoRepBench 是修复任务；SpreadsheetBench 是解析压力。
- 决定：按 N1/N2/N3/U 分层报告，不再把六个公开来源称作六份可互换的定位验证。
- 没有决定：没有把任何注入故障队列写成自然错误频率证据；没有把修复或解析指标
  与源定位 MRR 混合。
- 受保护数据：仅使用已揭晓清单和 Phase 0 审计；未读取任何最终盲测标签。
- 可复核依据：`research/V5_MODEL_DISCOVERY_DATA_LEDGER.json`。

## D0003：统计功效边界

- 日期：2026-08-31
- 依据：Phase 0 精确配对符号检验设计敏感性。
- 观察：25 个 Enron 工作簿组或 30 个最终模板组对 5 个百分点 Top-5 差异的最大
  保守检出功效约为 3.58% 和 4.01%。
- 决定：在候选锁前增加独立模板，或把结果明确标为估计/探索性证据；不在揭晓后
  降低 5 个百分点门槛。
- 没有决定：没有宣称当前盲测样本足以证明确认性优势；没有使用事件数替代结构组数。
- 受保护数据：功效计算未读取模型预测或盲测标签。
- 可复核依据：`research/V5_MODEL_DISCOVERY_POWER_REPORT.json`。

## D0004：下一步授权边界

- 日期：2026-08-31
- 当前状态：Gate 0 任务/功效冻结完成后进入 Gate 1；Gate 1 只完成文献卡和官方
  ExceLint 可运行性检查。
- 授权：可创建方法卡、基线适配器和无标签原子信号审计；可运行短测试。
- 未授权：新继任模型代码、连续权重搜索、扩大 Top-k、读取旧试用或新保管人数据。
- 下一条决策：只有 Gate 2A/2B 的固定外层测试结果出来后，才能在 A/B/C 中选一个分支。

## D0005：执行计划 v3 与核心假设重置

- 日期：2026-08-31
- 依据：Gate 0/1 回执、V5-Core/R2/PSL 失败审计和选择性基线诊断。
- 观察：旧研究线反复把结构异常、传播影响和修复响应合成单一排名分数；在单工作簿
  无真值条件下，合法多制度和信息不足案例不可由更多组件补出作者意图。
- 决定：当前主问题改为“多视角无标签证据能否安全支持选择性复核”；完整排名只有在
  固定 Gate 2 通过后才作为分支 A；否则转入 V4 系辅助或最小附加信息。
- 允许工作：无标签原子信号分片、确定性输出、独立评分器和短测试。
- 禁止工作：连续权重搜索、合并前人组件作为原创主张、读取两套 240+120、实现继任
  模型或用标签反向改写排名。
- 受保护数据：本决定未读取旧试用包或新保管人包。
- 可复核依据：`research/FORMULAGUARD_MODEL_DISCOVERY_PLAN_V3.md`、
  `research/V5_MODEL_DISCOVERY_GATE1_RESULT.json`、
  `research/V5_SUCCESSOR_BASELINE_DIAGNOSTIC_RESULT.md`。

## D0006：Gate 2 分支判定

- 日期：2026-08-31
- 依据：`results/model_discovery_gate2_final_v3/gate2_receipt.json`；196 个观测工作簿
  的无标签信号和 V4 基线均完整后才读取 220 个揭晓开发事件。
- 观察：固定 selector 的结构组宏 Top-5 在 Enron 为 38.67%（V4 为 52.00%），在
  Integer corpus 为 46.30%（V4 为 41.67%），在 Modified EUSES 为 71.67%（V4 为
  82.50%），在历史 100 为 84.74%（V4 为 62.11%）。选择性行动精度 62.93%、错误源
  行动覆盖 38.42%、控制行动率 36.67%。
- 决定：Gate 2A 和 2B 均不通过，选择分支 C；停止同一批标签上的连续权重、通道、
  Top-k 和规则搜索，进入四种最小附加信息的独立测量。
- 没有决定：没有创建 `V5-R1`，没有把 oracle selector 当成模型，没有把不同输入的
  旧 V4.2 输出伪造成同输入比较，也没有读取任何保管人 SECRET 数据。
- 论文边界：在 RQ-C 完成前只保留 `V4-R1` 为主模型；若 RQ-C 也不能形成安全增量，
  主结论应写为无标签可识别性边界与最小信息需求。

## D0007：RQ-C 最小附加信息结果

- 日期：2026-08-31
- 依据：`results/model_discovery_branch_c/branch_c_receipt.json` 和
  `research/V5_MODEL_DISCOVERY_BRANCH_C_RESULT.md`。
- 观察：一个输出格的依赖锥在 220 个事件上错误行动精度为 29.47%，控制行动率为
  100%；加入一个通过/失败位把控制行动率降为 0%，但错误源覆盖仍为 29.47%。在有
  核验历史版本的 90 个公开压力事件上，公式差异和单格精确约束均达到 100% 事件命中，
  这属于输入本身携带差异的上限。
- 决定：RQ-C 完成；不创建无标签继任模型，不把交互式上限改名为 `V5-R1`。论文主线
  保留 `V4-R1`，新增“无输出真值可识别性边界与最小信息需求”结果。
- 没有决定：没有读取保管人目录，没有把历史版本/约束上限与无标签成绩合并，没有在
  Gate 2 标签上继续搜规则或调参。

## D0008：跨工作簿角色先验的独立可行性门

- 日期：2026-08-31
- 依据：Gate 2 A/B 失败、RQ-C 完成；SheetJS Enron 公开仓库远程树元数据和
  `research/V5_CROSS_WORKBOOK_ROLE_PRIOR_PLAN.md`。
- 观察：Gate 2 只使用单工作簿内部证据；仓库中尚未检验过来自独立无标签真实工作簿
  的角色分布能否为单表内无同伴公式提供信息。SheetJS `enron_xls` 固定提交的
  `nuix/` 子集提供了一个规模受控、CC0 的候选无标签语料。
- 决定：在读取 SheetJS 工作簿内容前冻结 `CWRP` 自监督协议、模板去重、五折外层
  拆分、三项可用性/信息/校准门和失败停止规则。它是新的输入信息可行性实验，不恢复
  Gate 2 通道搜索，也不授权 `V5-R1` 名称。
- 没有决定：没有认定跨工作簿学习有效，没有实现定位候选，没有降低旧 Gate 2 门槛，
  没有把公式恢复任务当成错误定位结果。
- 受保护数据：未访问两套 `240+120`；未读取其路径、文件名、内容或标签。预注册前仅
  校验公开仓库 HEAD、远程树范围和许可证元数据，尚未物化 `nuix/` 工作簿内容。

## D0009：CWRP 解析覆盖阈值澄清

- 日期：2026-08-31
- 依据：实现转换清单时发现 D0008 预注册只写了“公式不可解析比例过高”，未给数值。
- 决定：在下载或打开任何 SheetJS 工作簿之前，将单工作簿进入语料的最低可解析公式
  比例固定为 50%，并要求至少有一个公式。该阈值不得依据 U0 结果修改。
- 没有决定：没有观察转换成功率、公式数量、公式语法或任何自监督成绩。
- 受保护数据：未访问；SheetJS 工作簿内容也尚未物化。

## D0010：DRFV 语料门失败

- 日期：2026-08-31
- 依据：`research/V5_DRFV_U0_RESULT.md` 和不可变 U0 回执。
- 观察：607 个去重工作簿和 219 个结构组达到门槛，但 74,570 个可解析公式低于
  100,000，且合格工作簿比例 22.93% 低于 80%。
- 决定：DRFV 在训练前终止，不降低 U0，不读取答案或 SpreadsheetBench 2。
- 没有决定：没有训练密度比模型，没有生成权重，没有授权 `V5-R1`。
- 受保护数据：未用于 U0。

## D0011：公开上下文模型审计与 FCRL 选择

- 日期：2026-08-31
- 依据：HERMES 提交 `e8a9f6c`、TUTA/ForTaP 提交 `4de8bba`、ACL 2023 HERMES
  论文、ACL 2022 ForTaP 论文及 SheetJS 固定树元数据。
- 观察：HERMES 公共仓库只有 README 和演示视频；ForTaP 有 MIT 代码与基础检查点，
  但预训练语料不公开且 Enron 微调权重与本项目安全集重合；EDRM 有 20,256 份原始
  工作簿但存在个人信息和 Enron 重合风险。
- 决定：预注册 FCRL。只允许冻结的 ForTaP 基础权重、SpreadsheetBench v1 input-only
  训练和新的反事实后悔值定位头；禁止 Enron 微调权重，暂不下载 EDRM 内容。
- 没有决定：没有声称 HERMES 可复现，没有把 ForTaP/Transformer/公式生成写成原创，
  没有恢复 DRFV，也没有授权正式 `V5-R1`。
- 受保护数据：公共来源审计期间发生一次路径级枚举偏差；没有读取文件、内容、标签或
  哈希。完整记录见 `research/PROTECTED_DATA_ACCESS_INCIDENT_20260831.md`。
- 可复核依据：`research/V5_FCRL_MECHANISM_SELECTION_AND_PREREGISTRATION.md` 和
  `research/V5_FCRL_PREREGISTRATION.json`。

## D0012：FCRL U0 工件与适配门通过

- 日期：2026-08-31
- 依据：`research/V5_FCRL_ADAPTER_IMPLEMENTATION_FREEZE.md`、
  `research/V5_FCRL_U0_RESULT.md` 和不可变 U0 回执。
- 观察：官方 base checkpoint 的 205/205 个骨干张量逐键/逐形状匹配；目标公式和缓存
  值改变不影响 encoder 哈希，两个独立进程哈希一致；冻结骨干 GPU 前向与官方 LSTM
  解码器反向均产生有限值。
- 兼容边界：官方 `FPTUTA` 因未声明 `torch_scatter` 不可直接导入；官方 LSTM attention
  存在 query 与 key/value 布局不一致，已在任何语料样本构造前固定为只转置 key/value。
- 决定：U0 通过，允许按预注册构建 SpreadsheetBench v1 input-only 训练样本并执行 U1。
- 没有决定：没有证明公式预测有效，没有运行定位、没有超过 V4、没有授权 `V5-R1`。
- 受保护数据：未列目录、未哈希、未读取、未用于 U0；U1-U4 和完整公开预测锁前继续
  不可访问。

## D0013：FCRL U1 语料覆盖门失败

- 日期：2026-08-31
- 依据：`research/V5_FCRL_U1_IMPLEMENTATION_FREEZE.md`、
  `research/V5_FCRL_U1_RESULT.md` 和本地不可变 U1 语料回执。
- 观察：607 个 input-only 工作簿的 96,375 个可见公式中，冻结适配器保留 11,635 个
  目标；train/calibration/internal-test 分别只有 82/20/23 个结构组有目标，低于固定
  的 153/33/33 全组覆盖要求。总体引用可达率为 91.87%，但它不能覆盖先决失败。
- 决定：FCRL 在 decoder 训练前终止；不删除空组、不扩大已观察语料上的解析器/函数
  集、不修改拆分或门槛，不运行 U1 internal-test、U2-U4 或保密评估。
- 没有决定：没有生成候选模型、没有计算 Top-1/Top-5、没有超过 V4 或公开方法的证据，
  没有授权 `V5-R1`。
- 受保护数据：未列目录、未哈希、未读取；此次运行只使用冻结的公开 input-only 语料。

## D0014：V4 残差上限通过与 V4-RRC 预注册

- 日期：2026-08-31
- 依据：`results/v4_residual_headroom_v0/receipt.json` 和
  `research/V5_V4_RESIDUAL_HEADROOM_RESULT.md`。
- 观察：保持 V4 前四位、只在第五位保留或替换时，单独 peer 候选的结构组宏 oracle
  空间总体为 +31.18pp、Enron 为 +22.67pp；全部四通道只再增加约 1.09pp。强制 peer
  Top-1 诊断总体为 +10.55pp，但 oracle 和强制规则均未回答交叉组控制风险。
- 决定：授权一个新的 `V4-RRC` 揭晓开发实验。它只能用 peer Top-5 候选、线性岭回归、
  五折结构组交叉拟合和风险阈值，V4 前四位不可改变；协议在任何实现和评分前提交。
- 修订边界：只有总体增益已过门而单独安全门失败时，允许一次预先写定的共识保护加严；
  不得增加特征/通道、改 Top-k、搜超参数或降低门槛。
- 没有决定：没有恢复 Gate 2A/2B，没有产生可部署成绩，没有授权 `V5-R1`，没有声称
  selective prediction、risk control、ridge 或 RRF 是本项目原创。
- 受保护数据：未列目录、未哈希、未读取。只有开发、旧 revealed-trial 和
  SpreadsheetBench 2 全部通过且完整公开预测锁已提交后，才使用用户已授权的一次
  保密评估。
- 可复核依据：`research/V5_V4_RESIDUAL_CONTROLLER_PREREGISTRATION.md` 和同名 JSON。

## D0015：V4-RRC D0 交叉拟合失败

- 日期：2026-08-31
- 依据：`results/v4_rrc_d0/receipt.json`、
  `results/v4_rrc_d0_failure_analysis.json` 和 `research/V5_V4_RRC_D0_RESULT.md`。
- 观察：五个校准折均无合格阈值；至少恢复 3 例时的最高正残差行动精度依次为
  25.00%、40.00%、31.82%、27.27% 和 57.14%，全部低于 75%。两进程投影哈希一致。
- 决定：D0 不通过；零行动使总体/Enron增益和恢复覆盖失败。由于失败不只属于允许
  加严的安全门，`single_revision_allowed=false`，不运行 D1，本研究线停止。
- 没有决定：没有把 oracle 上限写成模型结果，没有放宽精度/控制/损失门，没有生成
  候选或授权 `V5-R1`。同输入学术/历史基线按预注册另写补充回执，但不能改变 D0。
- 受保护数据：未列目录、未哈希、未读取；旧 revealed-trial、SpreadsheetBench 2 和
  保密 `240+120` 均未进入输入。

## D0016：AETR 完整排序迁移门失败

- 日期：2026-09-01
- 依据：`research/V5_AETR_RESULT.md` 和
  `results/aetr_crossfit_v1/receipt.json`。
- 观察：AETR 结构组宏 Top-5 相对 V4 提高 26.81pp、MRR 提高 0.2211，但 Enron
  Top-5 下降 10.67pp；完全留出 Enron 训练时 Top-5 下降 14.67pp、MRR 下降 0.1229。
  四项固定消融全部在 Enron 低于 V4，说明这是人工/N2 与自然错误机制迁移问题，不是
  单一特征组或权重实现错误。
- 决定：AETR 不冻结、不访问保密集；禁止 cohort 门控、Enron 特调权重、V4/AETR
  融合或新阈值补救。下一路线必须引入与自然纠错机制同源且与 Enron 测试隔离的监督，
  或明确改变推理输入契约。
- 没有决定：没有创建 `V5-R1`，没有把总体 N2 提升写成自然工作簿泛化优势。
- 受保护数据：未使用。

## D0017：VRER 预注册与 R0 初始批次

- 日期：2026-09-01
- 依据：预注册提交 `d9f32e0`、`research/V5_VRER_R0_INITIAL_RESULT.md` 和
  `results/vrer_r0_initial/reproduction_receipt.json`。
- 观察：独立获取的 `ewcole/tax_worksheets` 初始批次含 4 个公开作者确认纠错事件、
  8 个同址纠错公式，但保守合并后只有 1 个修订组和 1 个来源项目。解析率 100%，两个
  独立进程回执相同；事件、修订组、来源多样性和普通编辑控制规模仍未达到冻结 R0 门。
- 决定：R0 继续获取，但在 60 事件、80 格、40 修订组、10 项目及控制/多样性门全部
  通过前，禁止实现或训练 VRER。当前四个事件不得拆分成伪独立样本，也不得降低门槛。
- 没有决定：没有证明修订监督可学习、没有运行 Enron/公开压力、没有访问保密
  `240+120`、没有授权正式主模型名。
- 受保护数据：未列举、未哈希、未读取、未搜索、未用于获取或复算。

## D0018：VRER R0 最终失败并停止

- 日期：2026-09-01
- 依据：`research/V5_VRER_R0_FINAL_RESULT.md`、第二来源冻结提交 `6bf5017` 和
  `results/vrer_r0_batch_b/reproduction_receipt.json`。
- 观察：第二个有明确研究许可的来源 `eoframework/eof-solutions` 候选包含 28 个同址
  公式变化，超过工作簿级纠错声明最多 12 个直接变化的冻结上限；两个独立进程原始回执
  字节相同。30 个公开仓库的有界历史审计中，真实纠错线索主要因没有覆盖工作簿的明确
  许可证而不能进入语料；另一个 Apache-2.0 线索只新增工作簿，没有 before 版本。
- 决定：VRER R0 以 4 个事件、8 个纠错格、1 个修订组、1 个项目和 0 个控制结束，未
  达到 60/80/40/10/30 等冻结门；按协议在模型实现前停止。不拆分 28 格、不降低门槛、
  不使用无许可证文件，也不继续把开放式网络检索当作模型开发。
- 下一步边界：只允许另行命名并预注册一个改变推理输入契约的模型；首选契约为用户提供
  一个关键输出格及其期望值，以 peer 修复替换后的输出残差变化进行完整责任排序。
- 没有决定：没有证明 VRER 不可能、没有训练 VRER、没有授权 `V5-R1`、没有声称已经
  产生论文主模型突破。
- 受保护数据：未列目录、未哈希、未读取、未搜索、未用于第二批或来源审计。

## D0019：EORL 新输入契约预注册

- 日期：2026-09-01
- 依据：`research/V5_EORL_PREREGISTRATION.md` 及同名 JSON；冻结发生在任务构造、
  输出复算和 EORL 源码实现之前。
- 研究问题：用户提供一个关键公式输出格及其期望数值后，依赖锥内由 peer 生成的修复
  所造成的输出残差下降，能否形成比依赖距离、V4 锥内顺序、修复支持和最小编辑更准确
  的完整责任排序，并给出替换、路径和残差解释。
- 决定：只允许先运行 D0 可用性门；公开开发固定为现有 60 个错误配对和 30 个控制配对。
  D0 必须达到 40 个可用错误、12 个错误结构组、24 个控制，以及 24 个事件/8 个组的
  真实源 peer 修复残差可恢复性，并通过双进程和 LibreOffice 样本一致性。
- 模型复杂度：EORL v1 不训练权重，不使用神经网络、embedding、特征/阈值搜索或超参
  调优。只有确定性残差信号通过机制和分组公开门后，才有资格讨论新的学习模型；EORL
  失败本身不授权神经网络补救。
- 没有决定：没有构造 EORL 任务、没有读取任何输出值、没有实现排序、没有产生结果、
  没有授权 `V5-R1` 或保密评估。
- 受保护数据：仍禁止列目录、哈希、读取、搜索或重合审计；只有公开 D0/D1 通过并提交
  完整候选锁及保密协议后，才允许一次性运行。

## D0020：EORL v1 D0 覆盖失败并停止

- 日期：2026-09-01
- 依据：实现提交 `b31bef1`、去重身份修正 `f942b0b`、
  `research/V5_EORL_D0_RESULT.md` 和
  `results/eorl_d0/reproduction_receipt.json`。
- 观察：60 个公开错误中只有 26 个能构造满足冻结规则的受影响公式 sink 输出，只覆盖
  6/17 个结构组；30/30 控制可构造。26 个可用错误中 18 个真实源存在降低输出残差的
  冻结 peer 修复，覆盖 6 组，但未达到 24 事件/8 组门。两次运行的 tasks、scoring、
  cross-engine 和 receipt 全部字节相同；LibreOffice 抽样一致 11/12。
- 失败机制：34 个错误全部因为没有作为错误源严格后代的可见公式 sink；Info1 和
  Integer 的错误均为 0 个可用任务。18 个可恢复事件全部来自 Modified EUSES，其中
  17 个最佳内部修复将残差降至零，因此这是局部机制证据而非跨语料模型结果。
- 决定：EORL v1 在 D0 停止，不实现完整排序、不运行 D1、不降低 40/12/24/8 门、不把
  源位置 oracle 可恢复性写成定位精度，也不访问保密数据。
- 下一步边界：如果继续该研究问题，只能另行预注册“用户直接指定一个错误的中间输出”
  契约；不得把事后取消 sink 条件称为 EORL v1 修复。新路线仍须先证明跨语料任务覆盖
  和确定性机制，再实现排序。
- 没有决定：没有授权 `V5-R1`，没有证明期望输出机制普遍失败，也没有证明相对 V4 或
  学术基线有提升。
- 受保护数据：未列目录、未哈希、未读取、未搜索或用于任何 D0 工件。

## D0021：EORL 后验拓扑边界关闭中间输出延伸

- 日期：2026-09-01
- 依据：审计实现提交 `9a673de`、
  `results/eorl_d0/topology_followup_receipt.json`，回执 SHA-256
  `7cf2025f3de8ec5febe6d9140bd8abd19481b5ec282c02a2a283aa11f0ed6119`。
- 审计性质：这是 EORL 失败锁定后的只读后验诊断，不修改 D0 规则、门槛或结果，也不
  授权选择模型。审计只移除 sink 条件，检查已记录错误源是否存在任何公式后代。
- 观察：Info1 为 0/2、Integer 为 0/29、Modified EUSES 为 26/29；总计只有 26/60 个
  错误源具有任意公式后代，恰好等于 EORL 的可用事件数。未使用保密输入。
- 决定：D0020 中允许另行研究的“用户指定错误中间公式输出”不再作为当前公开配对上的
  下一路线，因为 Info1/Integer 共 31 个错误没有可指定的下游公式。不得通过取消 sink、
  改叫 IORR 或选择非后代公式来声称扩大覆盖。
- 下一步边界：下一候选必须采用与输出残差不同的学习目标，独立预注册数据边界、泄漏
  防护、有限校准、基线、消融及迁移门；EORL 证据只能作为已关闭路线的机制边界。
- 没有决定：没有证明所有用户指定输出任务都不可行，没有授权神经网络、`V5-R1` 或
  保密评估，也没有产生相对 V4 或公开学术基线的提升。

## D0022：PCRC 候选分布对齐修订预注册

- 日期：2026-09-02
- 依据：`research/V5_PCRC_PREREGISTRATION.md` 及同名 JSON；冻结发生在 PCRC 语料
  构建、训练或预测前。
- 必要纠正：仓库中已有 2026-08-31 的神经 semantic compatibility 双编码器。它的
  校准候选准确率 48.58%、选择性 internal-test 准确率 66.93%，均未过冻结门；揭晓
  public repair-margin Top-5 31.05%，低于 V4 的 57.89%。不得把同一想法换名重跑。
- 新假设：只检验冻结的可执行 peer repair 候选分布、目标遮蔽结构上下文和 repair-
  observed compatibility margin 是否能提供 V4 稳定晋升信号。网络、AST、peer、候选
  修复、masked prediction 和 selective promotion 都不是原创主张。
- 已知可行性：SpreadsheetBench v1 input-only 固定为 607 簿、219 组、74,570 公式；
  200-target 审计有 23,605 个目标。揭晓 public 60 错误的源上有 42 个 peer 候选、38
  个正确角色候选；两 peer 支持仍有 32 个，但控制同样常有候选，所以支持本身不判错。
- 决定：允许固定小型双向 GRU 双编码器、三档学习率校准和一次 S1/S2。S1 失败不得读
  public 预测；S2 失败不得接收 SpreadsheetBench v2 或保密数据。全部门通过仍只授权
  外部输入预测锁，不自动授权 `V5-R1`。
- 受保护数据：仍禁止列目录、哈希、读取、搜索或重合审计；历史非保密试用包不进入
  PCRC 训练。
