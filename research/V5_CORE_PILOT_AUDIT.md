# FormulaGuard V5-Core 240例pilot审计与唯一机制修订

状态日期：2026-08-28。本文区分原始pilot证据、机制诊断、修订后短回归和仍待重跑
的证据。原始pilot属于开发数据，不是独立验证，也不能作为论文最终性能结论。

## 1. 原始pilot完整性

- 240个错误工作簿、48个分层干净控制全部生成、预测和评分完成；错误集按6类错误、
  5种拓扑、4种公式制度和2个模板/种子精确平衡；干净集12种结构各4份。
- 错误与干净预测分别由24进程完成，完整排名审计通过，预测元数据中的
  `label_files_read`为空；评分发生在完成回执后。
- 数据审计硬门槛和跨划分审计通过；已揭晓85例和V4.3验证集明确排除。
- 原始运行Git提交为`8145a91b47eaf9d7252db64bc10b86e1a0ac8af9`，模型标识为
  `v5-core-dev-r1`。

关键证据SHA-256：

```text
summary.json                         d96d05d6d2c0acd6f56e068c340c7d5c2dfe21b826010e183cff15baf71895fc
dataset_audit.json                   98ec94d6464fe6ba31b35d0e0af15e0fd2232dcab92aecc6f1751344386dee37
config/training_audit.json           b303b4107d0641ec54eb79fd094870e3274060adcc0dd54b176d628535f81b25
predictions/prediction_complete.json 7d8ecc47c2ccb3eccc148dc63e95fabab0c618a0cff64e777cdab564ee815ad5
clean prediction completion          58dcf8232de2e523cd2b0b99cd0e055b8bfd88556ee36102e1412f37d2642a16
```

## 2. 原始结果及其使用边界

| 方法 | Top-5 | MRR | 最弱类型Top-5 | Coverage@32 | 精确修复 | 干净误报 |
|---|---:|---:|---:|---:|---:|---:|
| V4 | 95.83% | 0.5822 | 75.00% | 不适用 | 91.67% | 不适用 |
| V4.3 | 95.83% | 0.5848 | 75.00% | 不适用 | 95.83% | 不适用 |
| V5-Core-Rule r1 | 98.33% | 0.9637 | 90.00% | 100% | 95.83% | 8.33% |
| V5-Core-Learned r1 | 100% | 0.9667 | 100% | 100% | 91.67% | 4.17% |

学习头最终240例指标是在同一pilot上完成训练后计算，属于训练集内诊断；五折模板族
成对准确率98.33%只说明排序特征在这40个开发模板族上稳定，不能替代新的锁定验证。
规则头不依赖标签训练，但pilot仍是机制开发数据。两头的高分都不得外推为真实表格性能。

## 3. 失败机理

规则头仅有4个Top-5失败，全部集中于“周期结构 + 聚合函数替换”：

```text
v5c_pilot_0094  fanout       rank 11
v5c_pilot_0102  diamond      rank 6
v5c_pilot_0110  cross_sheet  rank 6
v5c_pilot_0118  mixed        rank 6
```

四例的真实模式均为`SUM/AVERAGE`交替，错误把一个`AVERAGE`改成`MIN`。旧周期检测使用
距离为2的直接比较；单个错误同时破坏两次比较，使11公式窗口的一致率从1降为7/9，
低于0.80阈值。因此错误段被当成普通列族，不同周期槽位被混作同伴：正确
`AVERAGE`虽在32候选内，但规则头选中`SUM`。这解释了“候选覆盖100%但精确修复和
Top-5仍失败”的差异，属于机制缺口而非候选数量不足。

## 4. 唯一一次机制修订

`v5-core-dev-r2`只做以下三项关联修改，不调整权重、阈值或门槛：

1. 周期2/3识别改为相对AST的槽位众数一致率，并要求不同槽位众数不同；
2. 仅用距离为周期整数倍的上下同伴生成`periodic_slot_consensus`候选，至少2票；
3. 只有当前公式与本槽众数一致时才增加周期合法例外概率，异常成员不受保护。

这是一项通用、无标签、可解释的周期制度修复。标签只用于定位失败组和重放排名，
模型路径没有接收源坐标、错误类型或正确公式。

## 5. 修订后短证据

- 新增“单个错误不破坏周期识别、同槽位产生正确候选、异常例外概率低于正常同槽
  公式”的回归测试；全仓库131项测试通过。
- 对原4个失败工作簿逐例回放，规则头均选择正确`AVERAGE`候选并把源公式排到第1。
- 重新运行24错误+24干净的smoke：规则头Top-5、MRR、Coverage@32和精确修复均为
  100%；规则头与学习头干净误报率、合法例外误报率均为0%。
- smoke仍只是工程证据；小样本满分不能证明泛化。

## 6. 决策

唯一一次pilot机制修订已经使用。原始r1 pilot结果保留哈希但不再代表当前r2模型。
必须完整重跑同一240错误+48干净pilot，确认周期修复没有扩大其他分层的误报或退化；
重跑后只能做审计和决定是否进入完整开发，不得再次修改核心机制。完整开发和480例
锁定验证仍是决定V5-Core能否取代V4的必要证据。
